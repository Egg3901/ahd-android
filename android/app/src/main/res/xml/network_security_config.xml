<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Defense in depth: block cleartext even if a misconfiguration re-enables it elsewhere. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
